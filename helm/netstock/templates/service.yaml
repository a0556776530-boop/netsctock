apiVersion: v1
kind: Service
metadata:
  name: {{ include "netstock.fullname" . }}
  labels:
    {{- include "netstock.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "netstock.selectorLabels" . | nindent 4 }}
